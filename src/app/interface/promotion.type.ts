export interface promotion {
    active?: boolean;
    startDate?: Date;   
    title?:string;
    description?:string;
    imageURL?:string;       
    endDate?: Date;    
    customerId?:string;    
    id:string;
    uid:string;
}